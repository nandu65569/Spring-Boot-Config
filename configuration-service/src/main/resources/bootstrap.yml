server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nandu65569/Spring-Boot-Config.git
          search-paths: ConfigInfo
  application:
    name: configuration-service


